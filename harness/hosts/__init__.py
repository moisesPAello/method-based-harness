"""Host renderers — one module per agent host (claude, later opencode, vscode).

A host renderer turns the host-neutral methodology + roles + profile into that host's
native files. Adding a host = one renderer that serves every methodology (N+M).
"""
